service firebase.storage {
  match /b/{bucket}/o {
    match /profilePics/{image} {
      allow read;
      allow write: if request.auth.uid == request.resource.name
                    && request.resource.contentType.matches('image/.*');
    }
  }
}
